function handleInfoChange() {
    const selectElement = document.getElementById('search-info');
    const selectedInfo = selectElement.value;
    alert('Вы выбрали: ' + selectedInfo);
}

function searchImages() {
    
}